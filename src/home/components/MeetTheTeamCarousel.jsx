import React from "react";

const VisibleDiv = (visible) => {
  return <div>{visible.map((el, i) => {

    

  })}</div>;
};

const MeetTheTeamCarousel = () => {
  return <div></div>;
};

export default MeetTheTeamCarousel;
